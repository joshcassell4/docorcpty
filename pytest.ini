[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --verbose
    --cov=orchestrator
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=80
asyncio_mode = auto